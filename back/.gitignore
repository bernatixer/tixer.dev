# Build artifacts
/target/

# Cargo lock is committed for binaries
# Cargo.lock

# SQLite database
*.db
*.db-journal
*.db-wal
*.db-shm

# Environment - .env.production is committed, local/dev are not
.env.local
.env.development
.env.production